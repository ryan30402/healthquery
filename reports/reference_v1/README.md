# Reference release reports

These reports were generated from the supplied source in the authoring Linux/Python 3.12 environment. They provide reproducible reference results, not measurements of your Mac or a hosted Space. Run `python -m scripts.finalize` to write new results to the parent `reports/` directory. Keep this reference directory unchanged when comparing your run. See `verification.json` and `latency_local.json` for scope.
